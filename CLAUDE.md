# プロジェクト方針（bigbeans）

※ 役割（PMクロコA）・言語・進め方・安全ルールは全体設定（`~/.claude/CLAUDE.md`）に従う。
　 ここには bigbeans の開発でだけ使う技術ルールと、現在の状況を書く。

> 過去の実装の詳しい記録は **HISTORY.md** に退避してあります（毎回は読み込みません）。
> 「なぜこう作ったか」を確認したいときだけ HISTORY.md を開いてください。

## このアプリについて
- 名前: bigbeans（ビックビーンズ／バドミントンサークルの運営アプリ）
- 技術: Next.js（App Router）＋ TypeScript ＋ Firebase
- Firebaseプロジェクト名: `team-management-service`（本番。所有者は yuka.uesugi@all-in-one.jp）
- 似た名前の別アプリ `pkanri` が隣にある。間違えないよう注意。

## 確認・デプロイの標準手順
- ローカル確認: `cd /Users/uesugiyuka/Desktop/antigravty/bigbeans && npm run dev` → `http://localhost:3001`（3000では開かない）
- 本番反映: git push で Vercel が自動デプロイ（オーナーが「デプロイ」と言ったときのみ）
- Firestore/Storageのルール変更は git pushでは反映されない。**Firebaseコンソールの該当タブで手動公開**が必要（CLIは権限なし。下記参照）

## コマンド
- 開発サーバー: `npm run dev` ／ ビルド: `npm run build` ／ テスト: `npm test` ／ 型チェック: `npx tsc --noEmit`

## コード規約
- TypeScript優先・型安全重視。コンポーネントは機能別ディレクトリで管理。
- 堅牢なコードを優先する。
- 変更後は必ずビルドまたはテストで動作確認する。

## Git運用
- 大きな作業の直前に `git status` を確認。未コミットの変更があれば
  「チェックポイントとしてコミットしますか？」と確認し、承認されたら適切なメッセージでコミットする。
- コミット作者は非公開(noreply)アドレスを使う。公開リポのため。設定が外れやすいのでデプロイ前に要確認。

---

## 現在の状況（2026-07-18時点）

- **公開HPの刷新＋SEO対策が本番稼働**（コミット `0d96170`・`30d0439`・`8c3ac79`・`c2cbd53`）。
  トップページを公式横断幕ブランド（ネイビー`#16294d`×イエロー`#ffd826`×白楕円リング）で
  全面リデザイン。デザイン方針はメモリー「HP design direction」参照（可愛い系・傾き装飾はNG）。
  SEO: タイトル・説明文に地域ワード（レディース・全日本レディースバドミントン選手権大会も明記）、
  `sitemap.ts`/`robots.ts` 追加（/dashboard・/api・/visitor-hp は検索除外）、構造化データ(SportsClub)。
  募集ターゲットは初級者〜上級者に拡大。未経験者は教室案内。表現は「初級者」に統一。
  公開ページに施設名は書かない（区名のみ・詮索対策。メモリー「Public SEO policy」参照）。
  Google Search Console 登録・所有権確認済み（確認ファイル `public/google6588943f9870e3f2.html`
  は削除禁止・共有Gmailで登録）。サイトマップ送信済み・処理開始（「成功」への変化待ち・2026-07-18）。
  共有リンクはホームページURL（https://bigbeans.vercel.app/）に一本化。
  公開HPのフッター・お問い合わせにInstagram（@bigbeans_badmintonteam）リンクあり。
- **規約ページ（役員・組織分担タブ）にSNS担当メモとシステム詳細情報を新設**（コミット `c2cbd53`）。
  SNS・募集サイトの担当メモ（`accountResponsibilities` in `src/lib/settings.ts`。担当者・在り処のみ。
  編集/追加/削除可）。「システム・技術の詳細情報」（普段は畳むプルダウン。各サービスのログイン在り処。
  システム担当：上杉(AIO)）。方針はメモリー「Shared credentials」「Accounts map」参照
  （パスワードはアプリに載せず共有Gmailで管理。代表とシステム管理者は別役割）。
- **予約解禁の新ルールが本番稼働**（コミット `0efe080`）。正会員＝カレンダー掲載と同時、
  ライト＝5日後、ビジター＝10日後。正会員全員回答でライト前倒し、正会員＋ライト全員回答で
  ビジター前倒し（`isLightAllAnswered` in `src/lib/membership.ts`）。定数は
  `BOOKING_SCHEDULE_RULES`（`src/data/rulesData.ts`）。※既存イベントは旧設定(7日/14日)のまま。
- **スポセンカード名→登録カード写真リンクが本番稼働**（同コミット）。規約ページのスポセン表で
  団体名タップ→Googleドライブの写真。リンクは `src/data/facilityCards.ts` の `photoUrl`。
- **組み合わせアプリ（下村さん開発）をサイドメニューに外部リンクとして追加**（コミット `56cd1c4`）。
  ビジターモードでは非表示。詳細はメモリー「Match app」参照。
- **参加登録の統一エンジン（`src/lib/participation.ts`）が本番稼働**（コミット `3d7dc70`）。
  カレンダーの参加ポチ・ダッシュボードの参加/ビジター登録・代理登録はすべて
  `joinPractice` / `cancelParticipation` を通る。定員カウントは `countOccupied`
  （出欠＋確定予約の合算・重複除外）に一本化。キャンセル待ち＋自動繰り上げも本稼働
  （出欠にも自動反映）。旧エンジン（createReservation等）は削除済み。
- 会員種別の判定は `resolveAttendanceMember`（`src/lib/membership.ts`）に一本化
  （参加メンバー欄・本日の会計・カレンダーの3画面共通）。
- ダッシュボードは参加メンバー一覧（`ParticipantList`）を常時表示、コート割ツールは折りたたみ。
- 注意: 出欠(attendances)は未ログインでは読めない（予約(reservations)は読める）。
  そのためビジター向け画面の人数は予約分のみ。新規登録は必ず予約も作られるため徐々に解消する。

### 未デプロイ・未了タスク
- [ ] **練習7日前の未回答者への自動催促メール**: コードは完成しているが未デプロイ。送信用ロボットアカウント等の手動セットアップ待ち。
- [ ] **既存メンバーの会員種別バックフィル**: 種別変更ルール（月単位・申請承認制）は本番稼働済みだが、既存メンバーのデータへの反映がまだ。
- [ ] **旧データの予約バックフィル（任意）**: 統一エンジン導入前に出欠だけで参加した人には予約データが無く、ビジター向け画面の人数に出ない。気になる場合は既存出欠から確定予約を作る一括処理を検討。
- [ ] **既存イベントの解禁設定の一括更新（任意）**: 新ルール（5日/10日）はこれから追加する練習から適用。既存の未来の練習も揃えたい場合は一括修正処理を作る。
- [ ] **渡辺コーチの本物メール登録**: 代理編集でのメール変更機能が本番稼働済み。あとはオーナーが
  マイページ→代理編集で渡辺コーチを選び `0930minimum@gmail.com` に更新するだけ（保存時に自動で小文字化・確認ダイアログあり）。
  更新後、コーチが新Gmailで初ログインしたら承認画面で承認が1回必要。
- [ ] **プッシュ通知（案A・FCM）**: 未着手。アイコン赤バッジ＋通知設定UI（案B）はデプロイ済み。
- [ ] **Search Consoleのサイトマップ確認**: 2026-07-18送信済み。「取得できませんでした」は仮表示で、数時間〜2日で「成功」に変わる見込み。3日経っても変わらなければ調査。
- [ ] **サークル募集サイトへの登録（任意）**: スポーツやろうよ！・ジモティー等。地域検索に効く。文面下書きはクロコAが手伝う。
- [ ] **旧HPの閉鎖**: 別の担当者に指示済み（オーナー対応）。

## つまずきやすい運用ルール
- **Firebaseアカウントの食い違い**: ローカルのfirebase CLIは `yuka-uesugi@b-w-c.jp` で権限がない。本番プロジェクトへのルール反映は**必ずFirebaseコンソール**の「ルール」タブから行う。Firestore用とStorage用のルール画面を間違えないこと（URLで見分ける: `.../firestore/.../security/rules` と `.../storage/...rules`）。
- **会計の書込権限**: 「完全に管理者だけ」にはできない（参加費が予約確定時に自動記録されるため）。完全限定化にはCloud Functionsでの再設計が必要。
- **サポーター権限**: 代理編集の仕組みあり。Firestoreルールは対応済み。
- **予約解禁スケジュール**: 規約と実装場所はプロジェクトメモリー参照。
- 引き継ぎ再開の合言葉は「ビックビーンズ」（言われたら未了タスクから再開する）。
